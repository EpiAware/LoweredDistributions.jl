import{_ as i,o as t,c as o,an as a}from"./chunks/framework.GScxl26e.js";const u=JSON.parse('{"title":"Extending LoweredDistributions","description":"","frontmatter":{},"headers":[],"relativePath":"developer/extending.md","filePath":"developer/extending.md","lastUpdated":null}'),n={name:"developer/extending.md"};function s(r,e,d,c,h,l){return t(),o("div",null,[...e[0]||(e[0]=[a('<h1 id="extending" tabindex="-1">Extending LoweredDistributions <a class="header-anchor" href="#extending" aria-label="Permalink to &quot;Extending LoweredDistributions {#extending}&quot;">​</a></h1><p>Most extensions belong in your own package, not in this one. <code>lower</code> dispatches on ordinary Julia types, and every backend entry point is a stub function you can add a method to from anywhere, so you do not need to be inside this repository to extend it. This page covers how to do that, and when we would instead host the extension here.</p><p>The lowering type hierarchy is locked. <a href="./@ref"><code>AbstractLowering</code></a>, <a href="./@ref"><code>AbstractChainTrick</code></a> and the four concrete representations (<a href="/previews/PR110/lib/public#LoweredDistributions.ErlangChain"><code>ErlangChain</code></a>, <a href="/previews/PR110/lib/public#LoweredDistributions.Coxian"><code>Coxian</code></a>, <a href="/previews/PR110/lib/public#LoweredDistributions.PhaseType"><code>PhaseType</code></a>, and the continuous-time Markov chain <a href="/previews/PR110/lib/public#LoweredDistributions.CTMC"><code>CTMC</code></a>) are the whole set, and neither extension point below adds a new one.</p><h2 id="Giving-a-distribution-an-exact-lowering" tabindex="-1">Giving a distribution an exact lowering <a class="header-anchor" href="#Giving-a-distribution-an-exact-lowering" aria-label="Permalink to &quot;Giving a distribution an exact lowering {#Giving-a-distribution-an-exact-lowering}&quot;">​</a></h2><p><a href="/previews/PR110/lib/public#LoweredDistributions.lower"><code>lower</code></a> dispatches on the <code>Distribution</code>&#39;s type, with a moment-matched <a href="/previews/PR110/lib/public#LoweredDistributions.phase_type"><code>phase_type</code></a> fit as the fallback (<code>lower(d::Distribution) = phase_type(d)</code> in <code>src/lower.jl</code>). A distribution with a known exact chain representation gets its own method, following <code>lower(d::Gamma)</code> and <code>lower(d::Exponential)</code>:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">LoweredDistributions</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">lower</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MyDistribution</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ErlangChain</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(d; kwargs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>This is ordinary Julia dispatch on your own type, so it needs no extension mechanism. Add the method wherever <code>MyDistribution</code> is defined.</p><h2 id="Adding-a-backend-from-your-own-package" tabindex="-1">Adding a backend from your own package <a class="header-anchor" href="#Adding-a-backend-from-your-own-package" aria-label="Permalink to &quot;Adding a backend from your own package {#Adding-a-backend-from-your-own-package}&quot;">​</a></h2><p>This is the path we expect most people to take, and the one we prefer.</p><p>Each backend entry point is a stub function declared and exported here with no method attached — <a href="/previews/PR110/lib/public#LoweredDistributions.ode_problem"><code>ode_problem</code></a>, <a href="/previews/PR110/lib/public#LoweredDistributions.petri_net"><code>petri_net</code></a>, <a href="/previews/PR110/lib/public#LoweredDistributions.jump_problem"><code>jump_problem</code></a>, <a href="/previews/PR110/lib/public#LoweredDistributions.reaction_system"><code>reaction_system</code></a>, <a href="/previews/PR110/lib/public#LoweredDistributions.linear_chain_reactions"><code>linear_chain_reactions</code></a>. They are part of the public API before any backend package is loaded, precisely so a downstream package can supply the method.</p><p>From your own package: 2. Take <code>LoweredDistributions</code> as a dependency, and your target backend as a <code>[weakdeps]</code> entry in <em>your</em> <code>Project.toml</code>.</p><ol start="3"><li><p>Register an extension in your <code>[extensions]</code> table, gated on both.</p></li><li><p>In that extension, add a method to the relevant stub dispatching on <a href="./@ref"><code>AbstractLowering</code></a>, or on a narrower type if the backend only applies to one branch of the hierarchy.</p></li></ol><p>Your method receives the canonical view every backend shares. Build on the public surface: <a href="/previews/PR110/lib/public#LoweredDistributions.PhaseType"><code>PhaseType</code></a> gives you <code>(α, S)</code>, <a href="/previews/PR110/lib/public#LoweredDistributions.CTMC"><code>CTMC</code></a> gives you the state set and transition rates, and <a href="/previews/PR110/lib/public#LoweredDistributions.lower"><code>lower</code></a><code>(dist, PhaseType)</code> gets you the canonical form in one type-stable step.</p><div class="tip custom-block"><p class="custom-block-title">The shared generator is currently internal</p><p>The four in-tree backends build on <code>_generator(m)</code> (<code>src/generator.jl</code>), which returns the full state-space generator <code>Q</code> and a default initial condition for any <a href="./@ref"><code>AbstractLowering</code></a> uniformly. It is not exported and not marked <code>public</code>, so an out-of-tree extension should not depend on it. If your backend genuinely needs the full generator rather than the phase-type view, please open an issue — that is a signal we should promote it to the public API rather than a reason to reach into internals.</p></div><p>If your extension is useful to you alone, or is specific to your modelling stack, keeping it in your package is the right end state. It stays on your release cycle and we do not gate your changes.</p><h2 id="When-we-would-host-it-instead" tabindex="-1">When we would host it instead <a class="header-anchor" href="#When-we-would-host-it-instead" aria-label="Permalink to &quot;When we would host it instead {#When-we-would-host-it-instead}&quot;">​</a></h2><p>We would take a backend in-tree when it has general utility — a widely used simulation or inference package that most users of this one would reasonably expect to work out of the box, rather than a bridge to a specific project&#39;s stack.</p><p>Open an issue first. The question is whether the backend is general enough to justify us carrying it, not whether the code is good.</p><p>If we agree to host it, <code>ext/LoweredDistributionsSciMLBaseExt.jl</code> is the shortest of the four and the best template. It wraps <code>_generator</code>&#39;s <code>Q</code>/<code>u0</code> as an <code>ODEProblem</code> in about a dozen lines. The in-tree checklist is then: 2. Add the target package as a <code>[weakdeps]</code> entry and register the extension in <code>[extensions]</code> in this package&#39;s <code>Project.toml</code>.</p><ol start="3"><li><p>Declare and export a stub function with a docstring in <code>src/</code>.</p></li><li><p>Write <code>ext/LoweredDistributions&lt;Backend&gt;Ext.jl</code>, building on <code>_generator</code>, or on <a href="/previews/PR110/lib/public#LoweredDistributions.PhaseType"><code>PhaseType</code></a>/<a href="/previews/PR110/lib/public#LoweredDistributions.CTMC"><code>CTMC</code></a> directly if the backend needs the phase-type view rather than the full generator.</p></li><li><p>Add a tutorial under <code>docs/src/getting-started/tutorials/</code>, mirroring the existing four backend tutorials, and register it in <code>docs/pages.jl</code>.</p></li><li><p>Check the new backend&#39;s output against the source distribution on the same delay every other backend tutorial uses.</p></li></ol>',20)])])}const b=i(n,[["render",s]]);export{u as __pageData,b as default};
