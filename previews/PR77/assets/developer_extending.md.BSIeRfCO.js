import{_ as i,o as t,c as a,an as o}from"./chunks/framework.DKzSnQwd.js";const b=JSON.parse('{"title":"Extending LoweredDistributions","description":"","frontmatter":{},"headers":[],"relativePath":"developer/extending.md","filePath":"developer/extending.md","lastUpdated":null}'),n={name:"developer/extending.md"};function s(r,e,d,c,l,h){return t(),a("div",null,[...e[0]||(e[0]=[o('<h1 id="extending" tabindex="-1">Extending LoweredDistributions <a class="header-anchor" href="#extending" aria-label="Permalink to &quot;Extending LoweredDistributions {#extending}&quot;">​</a></h1><p>There are two different ways to extend the package: giving a distribution family its own exact lowering, and adding a new backend. The lowering type hierarchy itself — <a href="./@ref"><code>AbstractLowering</code></a>, <a href="./@ref"><code>AbstractChainTrick</code></a>, and the four concrete representations (<a href="/previews/PR77/lib/public#LoweredDistributions.ErlangChain"><code>ErlangChain</code></a>, <a href="/previews/PR77/lib/public#LoweredDistributions.Coxian"><code>Coxian</code></a>, <a href="/previews/PR77/lib/public#LoweredDistributions.PhaseType"><code>PhaseType</code></a>, <a href="/previews/PR77/lib/public#LoweredDistributions.CTMC"><code>CTMC</code></a>) — is locked; every lowering is one of these four, so neither extension point adds a new representation type.</p><h2 id="Giving-a-distribution-an-exact-lowering" tabindex="-1">Giving a distribution an exact lowering <a class="header-anchor" href="#Giving-a-distribution-an-exact-lowering" aria-label="Permalink to &quot;Giving a distribution an exact lowering {#Giving-a-distribution-an-exact-lowering}&quot;">​</a></h2><p><a href="/previews/PR77/lib/public#LoweredDistributions.lower"><code>lower</code></a> dispatches on the <code>Distribution</code>&#39;s type, with a moment-matched <a href="/previews/PR77/lib/public#LoweredDistributions.phase_type"><code>phase_type</code></a> fit as the fallback (<code>lower(d::Distribution) = phase_type(d)</code> in <code>src/lower.jl</code>). A distribution with a known exact chain representation gets its own method, following <code>lower(d::Gamma)</code> and <code>lower(d::Exponential)</code>:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">LoweredDistributions</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">lower</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MyDistribution</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ErlangChain</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(d; kwargs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>This is ordinary Julia dispatch on your own type, so it needs no extension mechanism — add the method wherever <code>MyDistribution</code> is defined.</p><h2 id="Adding-a-new-backend" tabindex="-1">Adding a new backend <a class="header-anchor" href="#Adding-a-new-backend" aria-label="Permalink to &quot;Adding a new backend {#Adding-a-new-backend}&quot;">​</a></h2><p>The four built-in backends (Catalyst, SciMLBase, AlgebraicPetri, JumpProcesses) each follow the same shape: 2. A stub function with no method, declared and exported in the core package — e.g. <code>function ode_problem end</code> in <code>src/ode.jl</code> — so it is part of the public API even before the backend package is loaded.</p><ol start="3"><li><p>A package extension <code>ext/LoweredDistributions&lt;Backend&gt;Ext.jl</code>, gated on the backend as a weak dependency, adding the concrete method dispatching on <a href="./@ref"><code>AbstractLowering</code></a> (or a narrower type, if the backend only applies to one branch of the hierarchy).</p></li><li><p>The extension builds on the internal numeric core every backend shares: <code>_generator(m)</code> (<code>src/generator.jl</code>) returns the full state-space generator <code>Q</code> and a default initial condition for ANY <a href="./@ref"><code>AbstractLowering</code></a> uniformly, whether it came from a phase-type fit or a bare <a href="/previews/PR77/lib/public#LoweredDistributions.CTMC"><code>CTMC</code></a>.</p></li></ol><p><code>ext/LoweredDistributionsSciMLBaseExt.jl</code> is the shortest of the four and the best template to start from — it wraps <code>_generator</code>&#39;s <code>Q</code>/<code>u0</code> as an <code>ODEProblem</code> in about a dozen lines.</p><p>To add a fifth backend: 2. Add the target package as a <code>[weakdeps]</code> entry and register the extension in <code>[extensions]</code> in <code>Project.toml</code>.</p><ol start="3"><li><p>Declare and export a stub function with a docstring in <code>src/</code>.</p></li><li><p>Write <code>ext/LoweredDistributions&lt;Backend&gt;Ext.jl</code> following the pattern above, building on <code>_generator</code> (or on <a href="/previews/PR77/lib/public#LoweredDistributions.PhaseType"><code>PhaseType</code></a>/<a href="/previews/PR77/lib/public#LoweredDistributions.CTMC"><code>CTMC</code></a> directly, if the backend needs the phase-type view rather than the full generator).</p></li><li><p>Add a tutorial under <code>docs/src/getting-started/tutorials/</code>, mirroring the existing four backend tutorials, and register it in <code>docs/pages.jl</code>.</p></li><li><p>Check the new backend&#39;s output against the source distribution on the same delay every other backend tutorial uses.</p></li></ol>',12)])])}const u=i(n,[["render",s]]);export{b as __pageData,u as default};
