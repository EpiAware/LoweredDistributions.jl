# MANAGED by EpiAwarePackageTools.scaffold — do not edit by hand.
name: Test

on:
  push:
    branches:
      - main
  pull_request:
  merge_group:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    uses: EpiAware/.github/.github/workflows/tests.yml@2c7ce73459772f11dad46ac7f456e35f0d296faf
    # Package-owned override, preserved across scaffold_update (kit #73). The
    # package's Julia floor is 1.11, so `lts` (1.10) is dropped from the
    # reusable's default matrix rather than run against a version this package
    # no longer claims to support.
    with:
      julia_versions: '["1", "pre"]'
    secrets: inherit  # pragma: allowlist secret

  downgrade-compat:
    uses: EpiAware/.github/.github/workflows/downgrade.yml@2c7ce73459772f11dad46ac7f456e35f0d296faf
    # The oldest Julia the package supports, so the downgrade run resolves
    # against the floor it actually claims.
    with:
      julia_version: '1.11'
    secrets: inherit  # pragma: allowlist secret
