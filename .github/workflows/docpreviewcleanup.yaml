# MANAGED by EpiAwarePackageTools.scaffold — do not edit by hand.
name: Doc Preview Cleanup

on:
  pull_request:
    types: [closed]

permissions:
  contents: write

jobs:
  doc-preview-cleanup:
    uses: EpiAware/.github/.github/workflows/docs-preview-cleanup.yml@d024dc6927cc3523292a159dc0d4a45193c9c65d
    secrets: inherit  # pragma: allowlist secret
