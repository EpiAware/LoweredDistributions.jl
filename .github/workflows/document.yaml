# MANAGED by EpiAwarePackageTools.scaffold — do not edit by hand.
name: Documenter

on:
  push:
    branches: main
    tags: '*'
  pull_request:
  merge_group:
  release:
    types: [published]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  Documenter:
    uses: EpiAware/.github/.github/workflows/documentation.yml@d024dc6927cc3523292a159dc0d4a45193c9c65d
    secrets: inherit  # pragma: allowlist secret
